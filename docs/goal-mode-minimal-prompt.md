# Goal Mode Minimal Prompt

Read `docs/GOAL_MODE_CHECKPOINT.md` and
`docs/ml_v2_quant_hybrid_model_checkpoint.md`. Find the earliest incomplete ML
v2 checkpoint. Complete exactly one checkpoint, commit focused files only, and
stop. Keep production `BLOCK`, protected candidate `PAPER_REVIEW`,
`trading_allowed=False`, `candidate_promotion=False`,
`broker_submission=False`, `order_execution=False`, and
`production_effect=none`.
